(+) : Int -o Int -o Int;;
(MkInt# x) + (MkInt# y) = MkInt# (x +# y);;

(-) : Int -o Int -o Int;;
(MkInt# x) - (MkInt# y) = MkInt# (x -# y);;

fib_n : Int -> Int;;
fib_n n = case n of
    0 -> 1;
    1 -> 1;
    n -> fib_n (n - 1) + fib_n (n - 2);;

main : Int;;
main = fib_n 10;;

