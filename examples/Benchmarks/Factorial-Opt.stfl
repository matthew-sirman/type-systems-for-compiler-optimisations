(-) : Int -o Int -o Int;;
(MkInt# x) - (MkInt# y) = MkInt# (x -# y);;

(*) : Int -o Int -o Int;;
(MkInt# x) * (MkInt# y) = MkInt# (x *# y);;

fact_r : Int -> + Int;;
fact_r n = case +n of
    0 -> 1;
    n -> n * fact_r (n - 1);;

main : Int;;
main = fact_r 20;;

